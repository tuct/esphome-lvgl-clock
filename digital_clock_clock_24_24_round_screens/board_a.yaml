# ClockClock 24 - board A: wall column 0, mini-clocks 0 / 2 / 4.
# A listener. Everything else lives in common_slave.yaml.

packages:
  slave: !include common_slave.yaml

substitutions:
  clock_index_a: "0"
  clock_index_b: "2"
  clock_index_c: "4"
