# Copy to secrets.yaml (gitignored) before compiling board_a.yaml - it is the
# only board that reads these. board_b.yaml has no network and is flashed over
# USB.
wifi_ssid: "your-ssid"
wifi_password: "your-wifi-password"
ota_password: "your-ota-password"
