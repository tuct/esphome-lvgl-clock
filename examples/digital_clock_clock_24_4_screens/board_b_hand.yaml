# Board B (listener) on the older HAND-WIRED prototype instead of the carrier
# PCB. Identical to board_b.yaml except the four moved pins - see
# pinout_hand.yaml.
#
#   esphome run board_b_hand.yaml

esphome:
  name: cc24-4s-board-b-hand

packages:
  board: !include board_b.yaml
  pinout: !include pinout_hand.yaml
