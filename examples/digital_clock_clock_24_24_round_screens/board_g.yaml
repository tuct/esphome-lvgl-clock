# ClockClock 24 - board G: wall column 6, mini-clocks 18 / 20 / 22.
# A listener. Everything else lives in common_slave.yaml.

packages:
  slave: !include common_slave.yaml

substitutions:
  clock_index_a: "18"
  clock_index_b: "20"
  clock_index_c: "22"
